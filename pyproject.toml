[build-system]
requires = ["setuptools>=64"]
build-backend = "setuptools.build_meta"

[project]
name = "llmfetcher"
version = "0.4.0"
description = "Multi-agent LLM orchestration framework"
requires-python = ">=3.12"
authors = [
    {name = "LunaticLegacy"},
]
dependencies = [
    "openai>=1.0.0",
    "anthropic>=0.100.0",
    "litellm>=1.0.0",
    "markdown-it-py",
    "fastapi>=0.115.0",
    "uvicorn>=0.30.0",
]

[project.scripts]
llmfetcher = "llmfetcher.cli:main"
llmfetcher-web = "llmfetcher.webapp:main"

[tool.setuptools.packages.find]
include = ["llmfetcher*"]
